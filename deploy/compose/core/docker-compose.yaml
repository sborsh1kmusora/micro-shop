services:
  placeholder:
    image: alpine
    command:
      ["sh", "-c", "echo 'This is a placeholder service'"]
    networks:
      - microservices-net

networks:
  microservices-net:
    name: microservices-net
    external: false